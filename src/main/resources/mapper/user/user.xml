<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.runningmate.runningmate.user.mapper.UserMapper">

    <select id="selsectUserByEmail" resultType="com.runningmate.runningmate.user.entity.User">
        SELECT
            user_id
             , email
             , password
             , nickname
             , position_id
             , image_id
             , reset_token
             , level
             , create_date
             , update_date
        FROM user
        WHERE email = #{email}
    </select>

    <select id="selectUserById" resultType="com.runningmate.runningmate.user.entity.User">
      SELECT
        user_id
           , email
           , password
           , nickname
           , position_id
           , image_id
           , reset_token
           , level
           , create_date
           , update_date
      FROM user
      WHERE user_id = #{userId}
    </select>

    <insert id="insertUser" parameterType="com.runningmate.runningmate.user.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO
            user (
                email
                , password
                , nickname
                , position_id
                , image_id
                , level
                , create_date

            )
            VALUES(
                #{email}
                , #{password}
                , #{nickName}
                , #{position.positionId}
                , #{image.imageId}
                , #{level}
                , #{createDate}
            )
    </insert>
</mapper>